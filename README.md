# students-lk-spaghetti

## Сборка фронта

```bash
cd /home/spaghetti/spaghetti.w6p.ru/front && npm ci && npm run build && npm run reload
```

## Сборка бэка

```bash
cd /home/spaghetti/spaghetti.w6p.ru/back && composer install && php artisan migrate
```